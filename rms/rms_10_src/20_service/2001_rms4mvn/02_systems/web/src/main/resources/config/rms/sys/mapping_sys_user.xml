<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neoye.rms.domain.system.repo.ILoginRepo" >

	<resultMap type="com.neoye.rms.domain.system.intf.dto.UserDTO" id="userDTO">
		<result column="user_id" property="userId" jdbcType="INTEGER" />
		<result column="account" property="account" jdbcType="VARCHAR" />
		<result column="user_name" property="userName" jdbcType="VARCHAR" />
		<result column="password" property="password" jdbcType="VARCHAR" />
		<result column="sex" property="sex" jdbcType="INTEGER" />
		<result column="email" property="email" jdbcType="VARCHAR" />
		<result column="phone" property="phone" jdbcType="VARCHAR" />
	</resultMap>
	
	
	<select id="selectUserByUserPw" resultMap="userDTO"
		parameterType="com.neoye.rms.domain.system.intf.dto.UserDTO">
		SELECT u.user_id,u.account,u.user_name,u."password", u.email, u.phone, dc.item_name AS sex FROM rmsy_user_t u
		LEFT JOIN (SELECT * from rmsy_dic_common_t WHERE dic_type = 10001) dc ON dc.item_value = btrim(to_char(u.sex, '9')) 
		WHERE u.account = #{account}
	</select>
	
</mapper>